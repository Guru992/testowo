import { SortbysredniaPipe } from './sortbysrednia.pipe';

describe('SortbysredniaPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbysredniaPipe();
    expect(pipe).toBeTruthy();
  });
});
